'use client';
import { Session } from '../types';
export default function Ride({ initialSession, onReset }: { initialSession: Session; onReset: () => void }) {
  return <div className="p-8 text-white">Ride (TODO)</div>;
}
